
services:
  gcp-vision:
    build:
      dockerfile: Dockerfile.dev
      context: .
    env_file:
      - .env
    ports:
      - 9000:8080
    volumes:
      - ./src:/app/src
      - ./credentials.json:/credentials.json:ro
